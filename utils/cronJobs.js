// utils/cronJobs.js
const startCronJobs = () => {
  console.log('✅ Cron jobs started (placeholder)');
  // Add actual cron logic later (e.g., SP/Rider assignment retries)
};

module.exports = { startCronJobs };